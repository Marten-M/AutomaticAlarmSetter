<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AutomaticAlarmSetter" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.AutomaticAlarmSetter" parent="Base.Theme.AutomaticAlarmSetter" />

    <style name="Theme.AutomaticAlarmSetter.Fullscreen" parent="Theme.AutomaticAlarmSetter">
        <item name="actionBarStyle">
            @style/Widget.Theme.AutomaticAlarmSetter.ActionBar.Fullscreen
        </item>
        <item name="windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
    </style>

    <style name="ThemeOverlay.AutomaticAlarmSetter.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/light_blue_600</item>
        <item name="fullscreenTextColor">@color/light_blue_A200</item>
    </style>
</resources>